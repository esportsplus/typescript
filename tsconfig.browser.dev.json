{
    "compilerOptions": {
        "noEmit": true,
        "rootDir": "${configDir}"
    },
    "extends": "./tsconfig.browser.json",
    "include": [
        "${configDir}/*.config.ts",
        "${configDir}/bench/**/*",
        "${configDir}/src/**/*",
        "${configDir}/test/**/*"
    ]
}
