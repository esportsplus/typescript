# Changelog

## Completed
- follow-up discharged (completed) from spec-emit-contract-fidelity: uid determinism: src/compiler/uid.ts mints a per-process uuid() namespace, so em… — evidence: c86e439
- follow-up discharged (completed) from spec-emit-contract-fidelity: root export surface: src/index.ts is 'export {}' while package.json advertises '… — evidence: e1a0b52
- abandoned spend: ~$0.00 equivalent API cost across 0 terminal non-COMPLETED item(s); recorded, not divided into per-item costs (run cbf7c996)
- run-level spend: ~$3.17 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run cbf7c996)
- [pin-typescript-exact] b8c18cc3cb31ea42c2023ba47ad045dad539634d. Deviations: deviated — Used `pnpm install --lockfile-only` instead of a plain `pnpm install`: node_modules in this worktree is a symlink to the main checkout's shared D:\typescript\node_modules, and a plain install triggered an interactive 'remove and reinstall node_modules from scratch' prompt against that shared, out-of-worktree directory, which cannot be answered non-interactively and would risk destructive action outside worktree confinement. --lockfile-only produced the identical lockfile change (verified idempotent across 3 runs) without touching the shared node_modules.. Cost: ~$3.53 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- [escape-and-symbol-guards] aa05e81. Deviations: deviated — Rewrote the pre-existing 'returns false when getAliasedSymbol throws' test into 'a non-alias symbol reference returns false without relying on a thrown-and-swallowed error' (same assertion, mock now proves getAliasedSymbol is never invoked for a non-alias symbol) rather than leaving a stale throw-mock test whose narrative no longer matches the guard-based implementation.; added — Added a new true-branch test ('resolves a re-export through an aliased symbol whose flags carry the alias bit') exercising the SymbolFlags.Alias guard's positive path, since no existing test covered checker.getAliasedSymbol actually being called and returning a match.; ladder: 1 attempt (critic), completed. Cost: ~$13.35 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- [plugin-path-silent-failures] 09dc011ea668d53a08f6a5adff1631d8dc7f86ba. Deviations: deviated — Defect 1 step-1: replacing return true with return false failed only 3 mock-checker unit tests that encoded the old trust-the-name contract; imports.includes is a public API unexercised by any e2e/real-checker path, so no real resolution failed — treated as dead defensive code and the 3 unit tests were updated to a resolving mock checker (node_modules-path declaration) matching the settled contract.; deviated — Defect 3: took design step 3 (keep stripJsonc + add explicit extends resolution) because the probe proved api.parseConfigFile does not surface compilerOptions.plugins at all (step 2's precondition false).; deviated — Audit line numbers were stale (as warned): return true was at imports.ts:134-135 (accurate); loadPlugins branches cited at tsc.ts:186-204/192/200 were actually ~301-350; main plugin detection cited at tsc.ts:221-236 was ~352-395; stripJsonc cited at tsc.ts:286-402 was ~437-553. All located by symbol.; added — Added exported resolvePluginConfigs() plus internal extendsTarget()/readPlugins() helpers to src/cli/tsc.ts, and a 'resolvePluginConfigs extends resolution' describe block covering package-specifier and array extends forms plus a no-plugins-in-chain case.; ladder: 1 attempt (critic), completed. Cost: ~$43.38 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- [sourcemap-composition] 15b960e71b76973db6e6dcbb6441371e10be8c6e. Deviations: deviated — tests/cli/emit-contract.test.ts already existed (landed by the emit-mirror-contract dependency); the item listed it 'to be created' — I extended it with the e2e decoded-mapping case instead of creating it.; deviated — Composed .js.map omits sourcesContent (a Design-sanctioned option) to guarantee no mirror/transformed text leaks, rather than rewriting each entry to the original source text.; partial — toSourceMapV3 (Vite consumer) emits one segment per transformed line (line-granular, exact at line starts) since Rollup re-chains it; the exact per-column fidelity the Design requires is delivered in the emitted .js.map via arithmetic composition of tsc's per-token segments and is verified by the e2e column assertions.; ladder: 1 attempt (critic), completed. Cost: ~$85.82 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- [argv-flag-gating] a1b99b4. Deviations: deviated — Chose the design's 'thread the option explicitly' discretion path: build() gains a 4th param `noEmit = false` rather than reading process.argv inside build(). Consequence — no argv stub needed changing in either tsc.test.ts (line 179 --noEmit stub) or emit-contract.test.ts, contrary to the item's note anticipating stub repair; emit-contract cases already use `-p tsconfigPath` argv (not --noEmit) so were never at risk of silencing. Verified both files stay green.; added — Exported classifyFlags and main from src/cli/tsc.ts (test-surface only; package.json exports never exposes ./cli/*) to unit-test flag classification and the --watch behavioral path without process spawning.; deviated — Skipped the audit-fidelity git-checkout mutation-proof: this seat's CONFINEMENT rule prohibits `git checkout -- <path>`. Non-vacuity holds by construction (base tree cannot import classifyFlags/main; noEmit param absent; case A asserts out-dir absence which only holds when the noEmit block short-circuits before emit()).; ladder: 1 attempt (critic), completed. Cost: ~$20.54 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- [emit-mirror-contract] 9ccafe9. Deviations: none; ladder: 5 attempts (critic → fixer FIXED → critic → fixer FIXED → critic), completed. Cost: ~$57.76 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- [e2e-fixture-harness] 6c04d0d. Deviations: none; ladder: 5 attempts (critic → fixer FIXED → critic → fixer FIXED → critic), completed. Cost: ~$31.71 equivalent API cost (blended-rate approximation). · spec: spec-emit-contract-fidelity
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run cbf7c996)
- abandoned spend: ~$0.00 equivalent API cost across 0 terminal non-COMPLETED item(s); recorded, not divided into per-item costs (run 73077c18)
- run-level spend: ~$0.00 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run 73077c18)
- [reuse-tsgo-instance] ff901f6fa3b42f78519db08b75199feb803c0297. Deviations: deviated — Chose strategy (b): build() takes an optional injected API; the build describe block opens ONE API in beforeAll (cwd=os.tmpdir()) and closes it in afterAll. Reuse across per-case tmpDir projects proved reliable (24/24 green, two runs), so no fallback to (a); global exit/argv mocks left as-is since the cases stay sequential (no cross-case race).; deviated — The project-not-found branch now routes through teardown(snapshot, api, root, owned) instead of a bare api.close(), making it owned-aware and adding harmless snapshot + languageService.dispose(root) cleanup (root is absent from the languageService cache at that point, so dispose is a no-op).; ladder: 1 attempt (critic), completed. Cost: ~$16.88 equivalent API cost (blended-rate approximation). · spec: spec-cli-emit-hardening
- [overlay-mirror-emit] 46311d484e021c8f99a688fb2b8620a4e834a1aa. Deviations: deviated — Chose mechanism B full-mirror: copy every input into a temp mirror dir inside the project root (transformed content where applicable), emit via a derived `extends` tsconfig with rootDir=mirror and outDir=mirror/__emit, then fs.cpSync the produced artifacts into the real outDir. Mirror-inside-root keeps node_modules/lib resolution working for the spawned child; emit-into-mirror-then-copy keeps sourcemap `sources` relative refs (e.g. ../index.ts) identical to an in-place build. On SIGKILL only the temp mirror leaks — real source is never written.; deviated — emit() signature widened to emit(tsconfig, fileNames, transformedFiles, root, outDir); build() now passes fileNames, root, and program.getCompilerOptions().outDir ?? root. Internal (non-exported) function; permitted under the item's mechanism discretion.; partial — Layout fidelity is exact for the common/tested case (outDir set, declaration:true, no separate declarationDir). Not specially handled, noted as limitations: a project with an explicit declarationDir would still emit .d.ts to the real declaration dir during spawn (an OUTPUT write, not a source write — invariant 1 holds); path-alias imports resolve to the real (untransformed) files; input files outside the tsconfig dir (path.relative → '..') are not remapped.; ladder: 1 attempt (critic), completed. Cost: ~$12.70 equivalent API cost (blended-rate approximation). · spec: spec-cli-emit-hardening
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run 73077c18)
- abandoned spend: ~$113.37 equivalent API cost across 2 terminal non-COMPLETED item(s) (cli-tsgo-emit, build-green-gate); recorded, not divided into per-item costs (run ecdbdff2)
- run-level spend: ~$1.22 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run ecdbdff2)
- [root-export-surface] 506484962d52b19be498c7935620c7f853fe5ab8. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$6.30 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [vite-plugin-lifecycle] 21ca6f6211dfc48a343e841b99456fdc126466f0. Deviations: deviated — transform() destructures { checker, program } from languageService.update and passes { checker, program } to coordinator.transform, matching the Design's stated recipe exactly.; deviated — Test mock: Design's recipe (mock parse delegating to a real languageService via vi.importActual inside an async vi.mock factory) fails at runtime — the real parse() invokes the tsgo native API against a scratch tsconfig and throws 'failed to parse src/app.ts' in the sandboxed test environment (verified by running the scoped suite, 3 failures). Replaced with a lightweight fake SourceFile object (`{ fileName, text: content } as unknown as SourceFile`) returned directly from the parse mock — coordinator.transform is itself mocked and never inspects the SourceFile's real shape, so this satisfies the Acceptance (0 regressions, fallback/lifecycle covered) without requiring the real API.; added — Added closeBundle/closeWatcher property assertions to the 'creates VitePlugin with correct shape' test, and two new dedicated tests asserting each lifecycle hook calls languageService.dispose with the configured root — beyond the single lifecycle test the Design explicitly required, for full coverage of both hooks.; ladder: 1 attempt (critic), completed. Cost: ~$7.27 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [coordinator-api-reparse] fefb3fd92ca4014a74c10133e94cf3af29f48ef5. Deviations: deviated — parse() default fileName changed from bare 'bench.ts' to repo-qualified root+'/src/coordinator-bench-fixture.ts' (mirroring the test migration) — the real language-service rejects an unresolvable bare filename ('failed to parse bench.ts'); bench transform bodies unchanged.; added — Added afterAll(() => languageService.dispose()) and the path/root plumbing to mirror the migrated test harness and release the language-service process.; ladder: 1 attempt (critic), completed. Cost: ~$39.16 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [imports-node-handles] 9035576. Deviations: deviated — Design decision 1 listed importing `Identifier` and guard `isImportSpecifier`; both are unused after the redesign (isImportSpecifier is replaced by `handle.kind === SyntaxKind.ImportSpecifier`; the `isIdentifier` guard narrows `node` so no `Identifier` cast is needed). Omitted them to satisfy noUnusedLocals — resolves the Design's named discretion point (guard narrows sufficiently; zero any, no non-null assertions).; deviated — Test parse helper uses absolute `process.cwd() + '/src/test-imports.ts'` instead of the Design's literal `'test.ts'`; tsgo's languageService.parse requires an absolute in-config path (bare relative reproducibly threw '@esportsplus/typescript: failed to parse test.ts', 15/15 red). Matches the sibling tests/compiler/language-service.test.ts `root + '/src/...'` convention. Acceptance (0 regressions) outranks the stale literal.; deviated — Every stale line-number anchor in the Design (119/144/159/109-117/etc.) was located by symbol grep, not printed offset; all matched the shipped file (175 lines) with no drift.; ladder: 1 attempt (critic), completed. Cost: ~$15.64 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [ast-unstable-guards] eac571c. Deviations: deviated — Design step 2 for tests/compiler/ast.test.ts said parse(code) should call languageService.parse('test.ts', code) with the literal relative filename; that failed every parse call ('failed to parse test.ts') because languageService.parse resolves the file through a real tsgo scratch-project config whose files:[file] entry needs a path consistent with the overlay fs contents key, and every other real usage (tests/compiler/language-service.test.ts) passes an absolute path. Fixed by deriving fileName from process.cwd() + '/test.ts' (normalized to forward slashes), matching the existing convention; all 14 assertions remain byte-identical.; added — Test file's guard import list also needed isCallExpression, isClassDeclaration, isObjectLiteralExpression, isVariableStatement pulled in from typescript/unstable/ast/is (previously reached via the ts namespace import) — not explicitly enumerated in the Design's guard list but required for the predicate callsites the Design says stay byte-identical.; ladder: 1 attempt (critic), completed. Cost: ~$6.94 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [language-service-api-lifecycle] 5012bf7. Deviations: none; ladder: 3 attempts (critic → fixer FIXED → critic), completed. Cost: ~$69.53 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [types-plugin-contract] 806d853ae7f40b82d1773c3cdf9d873f767883c8. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$3.00 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [baseline-regression-gate] c711005. Deviations: deviated — package.json already had agent:test (vitest run) and agent:build (pnpm build) from an earlier commit (c1a28cd) that lacked the item tag; rather than re-editing already-correct content, landed an --allow-empty commit tagged [baseline-regression-gate] to give reconcile its required landed-proof, since git refuses a content commit with zero diff.; deviated — Q1 opt-in (typescript-legacy alias capture) not exercised — took the default no-dependency branch, so vitest.legacy.config.ts was never authored and no new devDependency was added.; ladder: 1 attempt (critic), completed. Cost: ~$16.12 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [cli-tsgo-emit] cced4c5 (d4/4; d1-d4 9ada3db/fc39a95/81eb108/cced4c5). SALVAGED by hand after run ecdbdff2 blocked it on FABLE_REPLAN exhaustion — cherry-picked the dangling directive commits onto main (branch engine/ecdbdff2/u7 had been deleted). Deviations: DESIGN DECISION 5 UNSATISFIED — emit() retains in-place fs.writeFileSync + restore()/SIGINT/SIGTERM handlers instead of the mandated overlay-mirror emit (matches original pre-migration behavior; SIGKILL-mid-emit source-corruption window unchanged); tracked as open follow-up + issue 20260723T101032Z. Verified: tsc green, 138/138 tests green. · spec: spec-typescript-7-migration
- [build-green-gate] 2427406. Verified by hand post-salvage: pnpm build (tsc && tsc-alias) exit 0; full suite 138/138 green across 9 files; prepare restored to `pnpm build`. · spec: spec-typescript-7-migration
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run ecdbdff2)
- abandoned spend: ~$13.78 equivalent API cost across 10 terminal non-COMPLETED item(s) (baseline-regression-gate, types-plugin-contract, language-service-api-lifecycle, ast-unstable-guards, imports-node-handles, coordinator-api-reparse, cli-tsgo-emit, vite-plugin-lifecycle, root-export-surface, build-green-gate); recorded, not divided into per-item costs (run 3c1d531f)
- run-level spend: ~$0.00 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run 3c1d531f)
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run 3c1d531f)
