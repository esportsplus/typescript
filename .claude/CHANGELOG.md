# Changelog

## Completed
- abandoned spend: ~$113.37 equivalent API cost across 2 terminal non-COMPLETED item(s) (cli-tsgo-emit, build-green-gate); recorded, not divided into per-item costs (run ecdbdff2)
- run-level spend: ~$1.22 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run ecdbdff2)
- [root-export-surface] 506484962d52b19be498c7935620c7f853fe5ab8. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$6.30 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [vite-plugin-lifecycle] 21ca6f6211dfc48a343e841b99456fdc126466f0. Deviations: deviated — transform() destructures { checker, program } from languageService.update and passes { checker, program } to coordinator.transform, matching the Design's stated recipe exactly.; deviated — Test mock: Design's recipe (mock parse delegating to a real languageService via vi.importActual inside an async vi.mock factory) fails at runtime — the real parse() invokes the tsgo native API against a scratch tsconfig and throws 'failed to parse src/app.ts' in the sandboxed test environment (verified by running the scoped suite, 3 failures). Replaced with a lightweight fake SourceFile object (`{ fileName, text: content } as unknown as SourceFile`) returned directly from the parse mock — coordinator.transform is itself mocked and never inspects the SourceFile's real shape, so this satisfies the Acceptance (0 regressions, fallback/lifecycle covered) without requiring the real API.; added — Added closeBundle/closeWatcher property assertions to the 'creates VitePlugin with correct shape' test, and two new dedicated tests asserting each lifecycle hook calls languageService.dispose with the configured root — beyond the single lifecycle test the Design explicitly required, for full coverage of both hooks.; ladder: 1 attempt (critic), completed. Cost: ~$7.27 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [coordinator-api-reparse] fefb3fd92ca4014a74c10133e94cf3af29f48ef5. Deviations: deviated — parse() default fileName changed from bare 'bench.ts' to repo-qualified root+'/src/coordinator-bench-fixture.ts' (mirroring the test migration) — the real language-service rejects an unresolvable bare filename ('failed to parse bench.ts'); bench transform bodies unchanged.; added — Added afterAll(() => languageService.dispose()) and the path/root plumbing to mirror the migrated test harness and release the language-service process.; ladder: 1 attempt (critic), completed. Cost: ~$39.16 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [imports-node-handles] 9035576. Deviations: deviated — Design decision 1 listed importing `Identifier` and guard `isImportSpecifier`; both are unused after the redesign (isImportSpecifier is replaced by `handle.kind === SyntaxKind.ImportSpecifier`; the `isIdentifier` guard narrows `node` so no `Identifier` cast is needed). Omitted them to satisfy noUnusedLocals — resolves the Design's named discretion point (guard narrows sufficiently; zero any, no non-null assertions).; deviated — Test parse helper uses absolute `process.cwd() + '/src/test-imports.ts'` instead of the Design's literal `'test.ts'`; tsgo's languageService.parse requires an absolute in-config path (bare relative reproducibly threw '@esportsplus/typescript: failed to parse test.ts', 15/15 red). Matches the sibling tests/compiler/language-service.test.ts `root + '/src/...'` convention. Acceptance (0 regressions) outranks the stale literal.; deviated — Every stale line-number anchor in the Design (119/144/159/109-117/etc.) was located by symbol grep, not printed offset; all matched the shipped file (175 lines) with no drift.; ladder: 1 attempt (critic), completed. Cost: ~$15.64 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [ast-unstable-guards] eac571c. Deviations: deviated — Design step 2 for tests/compiler/ast.test.ts said parse(code) should call languageService.parse('test.ts', code) with the literal relative filename; that failed every parse call ('failed to parse test.ts') because languageService.parse resolves the file through a real tsgo scratch-project config whose files:[file] entry needs a path consistent with the overlay fs contents key, and every other real usage (tests/compiler/language-service.test.ts) passes an absolute path. Fixed by deriving fileName from process.cwd() + '/test.ts' (normalized to forward slashes), matching the existing convention; all 14 assertions remain byte-identical.; added — Test file's guard import list also needed isCallExpression, isClassDeclaration, isObjectLiteralExpression, isVariableStatement pulled in from typescript/unstable/ast/is (previously reached via the ts namespace import) — not explicitly enumerated in the Design's guard list but required for the predicate callsites the Design says stay byte-identical.; ladder: 1 attempt (critic), completed. Cost: ~$6.94 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [language-service-api-lifecycle] 5012bf7. Deviations: none; ladder: 3 attempts (critic → fixer FIXED → critic), completed. Cost: ~$69.53 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [types-plugin-contract] 806d853ae7f40b82d1773c3cdf9d873f767883c8. Deviations: none; ladder: 1 attempt (critic), completed. Cost: ~$3.00 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [baseline-regression-gate] c711005. Deviations: deviated — package.json already had agent:test (vitest run) and agent:build (pnpm build) from an earlier commit (c1a28cd) that lacked the item tag; rather than re-editing already-correct content, landed an --allow-empty commit tagged [baseline-regression-gate] to give reconcile its required landed-proof, since git refuses a content commit with zero diff.; deviated — Q1 opt-in (typescript-legacy alias capture) not exercised — took the default no-dependency branch, so vitest.legacy.config.ts was never authored and no new devDependency was added.; ladder: 1 attempt (critic), completed. Cost: ~$16.12 equivalent API cost (blended-rate approximation). · spec: spec-typescript-7-migration
- [cli-tsgo-emit] cced4c5 (d4/4; d1-d4 9ada3db/fc39a95/81eb108/cced4c5). SALVAGED by hand after run ecdbdff2 blocked it on FABLE_REPLAN exhaustion — cherry-picked the dangling directive commits onto main (branch engine/ecdbdff2/u7 had been deleted). Deviations: DESIGN DECISION 5 UNSATISFIED — emit() retains in-place fs.writeFileSync + restore()/SIGINT/SIGTERM handlers instead of the mandated overlay-mirror emit (matches original pre-migration behavior; SIGKILL-mid-emit source-corruption window unchanged); tracked as open follow-up + issue 20260723T101032Z. Verified: tsc green, 138/138 tests green. · spec: spec-typescript-7-migration
- [build-green-gate] 2427406. Verified by hand post-salvage: pnpm build (tsc && tsc-alias) exit 0; full suite 138/138 green across 9 files; prepare restored to `pnpm build`. · spec: spec-typescript-7-migration
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run ecdbdff2)
- abandoned spend: ~$13.78 equivalent API cost across 10 terminal non-COMPLETED item(s) (baseline-regression-gate, types-plugin-contract, language-service-api-lifecycle, ast-unstable-guards, imports-node-handles, coordinator-api-reparse, cli-tsgo-emit, vite-plugin-lifecycle, root-export-surface, build-green-gate); recorded, not divided into per-item costs (run 3c1d531f)
- run-level spend: ~$0.00 equivalent API cost (warm implementer runs + unit-scoped + boundary seats; never divided across items; blended rate = mean(input, output, cache_read, cache_creation) per million tokens) (run 3c1d531f)
- cost source of truth: ccusage per-model breakdown (local transcripts carry the input/output/cache split the run journal does not) — figures above are a blended-rate approximation per contracts/models.json pricing (run 3c1d531f)
